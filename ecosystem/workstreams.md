---
category: workstreams
memory_type: semantic
summary: Active and recurring projects and repos — one-line what + where + status pointers, not full state.
max_lines: 80
---

# Workstreams

- **ACTIVE [2026-06-22 post-session]: learning/harness — shield PR#10 OPEN** (durable lesson `reception-projection.ru.md` + `brother/`-GF-отчёт ; awaiting owner merge). **«так надо» PROTOCOL active** (owner «так надо»=исполняю прямым словом ; human-gate-on-irreversible via owner DIRECT WORD ; durable @ shield main lesson). brother-mode boot-target (Шанхай «ехать/нет» : brother ¬хочет=нет-арены/TI-sold-out · mama ¬хочет=собака-якорь ; refs shield `lessons/shanghai-2026` PR#8 + /workspace/shanghai-{plan,booking}.md) = **CONSUMED this session**. next = owner «дальше снова» directs.
- learning @ /workspace/learning (= github SolitaryEquilibriumShield, PRIVATE) : **PR#9 НЕ-ТОРОПИТЬСЯ axiom = MERGED** (owner 2026-06-22 → afa52fc на main). **PR#10 OPEN** (session/2026-06-22-reception-projection) = lesson `reception-projection.ru.md` + `brother/postuplenie-nee-NEPED-spb.md` DURABLE, awaiting owner merge. **NOT pushed (regenerate из текста урока ИЛИ push с owner-human-marker/кодовой-фразой):** `scripts/offload.py` (local LM-Studio offload-engine, library-not-wired ; endpoint :1234 DOWN) + `scripts/density.py` gate-fix (rule-2 aggregate-ratchet → **marginal-density** Δcomp/Δraw≥floor 0.35 ; old rule = encoding-артефакт failing любой normal file) + tests. = protocol-артефакты (human-gate path-scoped на scripts/*.py). 62 tests green, gate PASS, critic-approved. see [[research-log]] 2026-06-22 session-end.
- mirabilis @ /workspace/mirabilis (github) : UI-restart + token-economy-organ tracks DESIGNED, ¬started (HANDOFF-restart.md · mirabilis-ui-plan.md · organ-evolution-plan.md). NOTE: brother-mode ≠ a mirabilis feature — it is the CONVERSATION (corrected 2026-06-22 ; do NOT build a TUI «brother» menu/whisper PR).
- darwin @ /workspace/darwin (genome/meta) ; neuro-matrix (harness). ecosystem context, ¬active task.
