# Vec — transient working-state of Γ (open fixes · bugs · migrating issues)

The transient tier of Γ (§G). Holds the OPEN work directed evolution is currently consuming. **Self-pruning**: implemented → discarded. NOT the foundation (CORE/BODY), NOT the intermediate `dept/` (theory + graph-plans live there, preserved), NOT logs (session-dumps / critic-findings are SCRATCH, outside Γ — `genome ≠ archive`).

## Type
Every Vec entry is transient and not truth — it is neither a gene 𝒢, nor an archive entry Λ, nor DEPT. Once a fix or realization lands, the entry is obsolete and is discarded; discarding it never forces a CORE rebuild (independent lifecycle, §E). Raw session-dumps and critic-findings are NOT Vec and NOT Γ at all — they are SCRATCH, exported as entropy.

## Members
```
v-bugs   bugs.md          open fixes: N eval-gate (N7 stack-coverage [ROADMAP-vs-code conflict] · eval power N=17) + M §M-iter OPEN residue (dependabot manual-bump blindspot · env-sensitive PTY/signal tests · sanitize-overstrip HYPO)
v-issue  idea-exchange.md the cross-AI advisory exemplar migrated (public-Γ exemplar); durable patterns → COMPRESS into Λ.coding-practices, then discard the raw thread
```
v-plan (fix-graph.md) DISCARDED [§G self-prune] — superseded sandbox plan; W1–W7 landed (mirabilis PR#134), W8/W9/W10 live in dept/graph-plan-sandbox, §D-1/§D-2 defects resolved in §H / by W1 landing integration-only.
Moved out of Vec by the stability-tier restructure: `theory-of-everything` (the R→S audit) → `dept/` (intermediate, preserved); `session-dump` → SCRATCH (`/workspace/`, short-lived checking only).

## Role in evolution (§D)
An active entry feeds variation V (the working-set); once implemented it is discarded, leaving CORE/BODY/DEPT untouched. Vec is the **working-set**, not the **heredity** (heredity = CORE/BODY + git). Discarding is cheap and local.

## Ledger
FACT: bugs + the cross-AI advisory exemplar exist; the discard-after-implementation policy is now EXERCISED — v-plan (fix-graph) discarded on W1–W7 landing (mirabilis PR#134). ASSOC: the transient-tier assignment is the owner's content model.
